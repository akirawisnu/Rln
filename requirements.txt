# Base requirements — 'lite' build tier.
# For offline-NLP add:  pip install -r requirements-offline.txt
# For neural-NLP add:   pip install -r requirements-full.txt

# Core data/dataframe
pandas>=2.0.0
numpy>=1.24.0

# File I/O
openpyxl>=3.1.0
xlrd>=2.0.0
pyreadr>=0.5.0
dbfread>=2.0.7
pyarrow>=12.0.0

# Fuzzy matching (fuzzmerge)
polyfuzz>=0.4.0
rapidfuzz>=3.0.0

# TUI and REPL
textual>=0.80.0
rich>=13.0.0
prompt_toolkit>=3.0.0

# HTML import
lxml>=4.9.0
html5lib>=1.1

# Stats / econometrics
statsmodels>=0.14.0
scipy>=1.10.0
matplotlib>=3.7.0
linearmodels>=5.0

# Causal inference (didregress)
diff-diff>=3.0.0

# Larger-than-RAM mode (lrtm)
polars>=0.20.0

# Build tooling
pyinstaller>=6.0.0
